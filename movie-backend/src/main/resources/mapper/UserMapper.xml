<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.movie.mapper.UserMapper">

    <resultMap type="com.movie.entity.User" id="BaseResultMap">
        <id property="userId" column="user_id" javaType="java.lang.Long"/>
        <result property="userName" column="user_name" javaType="java.lang.String"/>
        <result property="userPwd" column="user_pwd" javaType="java.lang.String"/>
        <result property="userEmail" column="user_email" javaType="java.lang.String"/>
        <result property="userRole" column="user_role" javaType="java.lang.Integer"/>
        <result property="userHeadImg" column="user_headImg" javaType="java.lang.String"/>
    </resultMap>

    <select id="findUserById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT user_id, user_name, user_pwd, user_email, user_role, user_headImg
        FROM user 
        WHERE user_id = #{userId}
    </select>

    <select id="findUserByName" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT user_id, user_name, user_pwd, user_email, user_role, user_headImg
        FROM user 
        WHERE user_name = #{userName}
    </select>

    <select id="findUserLikeName" parameterType="java.lang.String" resultMap="BaseResultMap">
        SELECT user_id, user_name, user_pwd, user_email, user_role, user_headImg
        FROM user 
        WHERE user_name LIKE CONCAT('%', #{keyword}, '%')
    </select>

    <select id="findAllUser" resultMap="BaseResultMap">
        SELECT user_id, user_name, user_pwd, user_email, user_role, user_headImg
        FROM user
        ORDER BY user_id DESC
    </select>

    <insert id="addUser" parameterType="com.movie.entity.User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user(user_name, user_pwd, user_email, user_role)
        VALUES(#{userName}, #{userPwd}, #{userEmail}, 0)
    </insert>

    <update id="updateUser" parameterType="com.movie.entity.User">
        UPDATE user
        <set>
            <if test="userName != null and userName != ''">
                user_name = #{userName},
            </if>
            <if test="userPwd != null and userPwd != ''">
                user_pwd = #{userPwd},
            </if>
            <if test="userEmail != null and userEmail != ''">
                user_email = #{userEmail},
            </if>
            <if test="userRole != null">
                user_role = #{userRole},
            </if>
            <if test="userHeadImg != null and userHeadImg != ''">
                user_headImg = #{userHeadImg},
            </if>
        </set>
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteUser" parameterType="java.lang.Long">
        DELETE FROM user WHERE user_id = #{userId}
    </delete>

</mapper>
